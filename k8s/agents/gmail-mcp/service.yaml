apiVersion: v1
kind: Service
metadata:
  name: gmail-mcp
  namespace: agents
  labels:
    app: gmail-mcp
    type: mcp-server
spec:
  type: ClusterIP
  selector:
    app: gmail-mcp
  ports:
    - port: 3000
      targetPort: 3000
      name: sse
